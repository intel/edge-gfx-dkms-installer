#pragma once

/*
 * Newer i915 snapshots include this via <drm/clients/drm_client_setup.h>,
 * but older kernels (e.g. 6.12) ship it as <drm/drm_client_setup.h>.
 */

#include <drm/drm_client_setup.h>
